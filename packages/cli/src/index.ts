/**
 * @otter-agent/cli — CLI binary entrypoint.
 *
 * This package provides the `otter` command that launches an RPC-mode
 * agent session over stdio. Component loading, arg parsing, and
 * auth-from-env are handled here. The RPC protocol, handler, and
 * transport live in @otter-agent/rpc.
 */
export {};
